# Task 描述：T20 + T21（收尾）

这是 **TDD Coding Harness** 项目的最后两个 Task，可以并行。

---

## 背景

全部 19 个 Task 已完成，210 个测试通过，3 个 demo 可运行。

**工作目录：** `E:\MyClass\2-2-summer\AI-Enabled_SoftwareEngineerBootcamp\homework\`

---

## T20：README 完善（SP1）

**目标：** 补全 README.md 缺少的章节。

**涉及文件：**
- `README.md`（修改）

当前 README 已有：Quick Start、Design Decisions、目录结构。

**需要补充的章节：**

### 1. Architecture

用文字描述系统架构（不需要 ASCII 图），说明 CLI → Config → Provider → Loop → Tools/Feedback 的流程。

### 2. Configuration

说明 `config.yaml` 的配置方式：

```yaml
provider:
  name: openai          # mock | openai
  model: gpt-4o
  base_url: https://api.openai.com/v1
```

配置优先级：CLI 参数 > 配置文件 > 默认值。

### 3. Provider 切换

展示如何在不同模型间切换（只改配置文件，不改代码）：

```bash
# 使用 Mock（离线测试）
tdd-harness run "task" --provider mock

# 使用 OpenAI 兼容 API
tdd-harness run "task" --provider openai --model deepseek-v4-pro
```

### 4. Demo

```bash
# Guardrail 演示
python examples/demo_guardrail.py

# Feedback 分类演示
python examples/demo_feedback.py

# 完整 TDD 闭环演示
python examples/demo_autonomous_repair.py
```

### 5. 项目结构

更新已有目录结构，反映完整的当前状态（所有模块）。

### 6. 安全说明

API Key 通过环境变量 `OPENAI_API_KEY` 配置，绝不硬编码在代码中。`.env` 文件已被 `.gitignore` 排除。

### 7. Known Issues / Limitations

- 仅支持 OpenAI 兼容 API（OpenAI、DeepSeek、Qwen 等）
- 默认测试框架为 pytest
- Windows 与 Linux 的 Shell 命令差异可能影响 RunShell 工具
- 无 GUI / IDE 插件

---

## T21：AGENT_LOG + 最终验证（SP1）

**目标：** 完成 AGENT_LOG.md，运行最终验证。

**涉及文件：**
- `AGENT_LOG.md`（新建）
- `REFLECTION.md`（新建，可选）

### AGENT_LOG.md 格式

按时间顺序记录每个 Task 的关键信息，每条包含：

```markdown
## Task Tn — 名称

- **Date:** 2026-07-07
- **Sub-agent:** [模型标识]
- **Files:** [涉及文件列表]
- **Key decisions:** [实现中的关键设计决策]
- **Result:** [测试结果]
- **Reflection:** [遇到的问题和教训]
```

### 需要记录的 Task

使用 git log 查看所有 commit，为每个 Task（T1-T19）写一条记录。

```bash
git log --oneline --stat
```

### 最终验证

运行以下命令并确认全部通过：

```bash
D:\Python3.12\python.exe -m pytest src/tests/ -v
D:\Python3.12\python.exe examples/demo_guardrail.py
D:\Python3.12\python.exe examples/demo_feedback.py
D:\Python3.12\python.exe examples/demo_autonomous_repair.py
```

---

## DoD

```
□ README 包含 Architecture / Configuration / Provider切换 / Demo / 安全说明 / Known Issues
□ AGENT_LOG 覆盖所有 Task（T1-T19）
□ 最终验证：210 tests passing, 3 demos passing
```