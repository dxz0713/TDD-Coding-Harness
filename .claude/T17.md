# Task 描述：T17 — FeedbackEngine + 自适应修复策略

这是 **TDD Coding Harness** 的**主要贡献模块**——完整的反馈闭环引擎。

---

## 背景

已完成：T1-T16（全部基础模块 + Collector + FailureAnalyzer）。

**工作目录：** `E:\MyClass\2-2-summer\AI-Enabled_SoftwareEngineerBootcamp\homework\`

**测试运行：** `D:\Python3.12\python.exe -m pytest src/tests/ -v`

**已有模型的 feedback 模块（`src/harness/models.py`）：**

```python
class FailureType(str, Enum):
    SYNTAX_ERROR = "syntax_error"
    IMPORT_ERROR = "import_error"
    ASSERTION_ERROR = "assertion_error"
    TIMEOUT = "timeout"
    RUNTIME_ERROR = "runtime_error"
    TEST_FAILURE = "test_failure"
    UNKNOWN = "unknown"

class AnalysisResult(BaseModel):
    failure_type: FailureType
    location: str | None = None
    error_message: str = ""
    assertion_expected: str | None = None
    assertion_actual: str | None = None
    raw_snippet: str | None = None

class Feedback(BaseModel):
    failure_type: FailureType
    summary: str
    details: AnalysisResult
    repair_prompt: str
```

**已有 Collector + FailureAnalyzer（`src/feedback/analyzer.py`）：**

```python
class Collector:
    @staticmethod
    def collect(result: ToolResult) -> CollectedOutput: ...  # ANSI stripping, failed test extraction

class FailureAnalyzer:
    def analyze(self, collected: CollectedOutput) -> AnalysisResult: ...  # 7 FailureType classification
```

---

## T17：FeedbackEngine + 自适应修复策略（SP2）

**目标：** 实现完整的反馈引擎，连接 Collector + Analyzer + 自适应修复策略映射。

**涉及文件：**
- `src/feedback/engine.py`（新建）
- `src/feedback/strategies.py`（新建）
- `src/tests/test_feedback_engine.py`（新建）

### 设计要点

1. **`FeedbackEngine`** 是入口类，调用 Collector → Analyzer → Strategy 链条
2. **`RepairStrategy` 抽象基类**，每种 FailureType 对应一个具体策略类
3. **使用策略映射**：`strategy_map: dict[FailureType, RepairStrategy]`，Engine 根据 `analysis.failure_type` 查找对应策略
4. 成功结果（exit_code=0）返回 `None`，表示无需修复
5. 新增 FailureType 只需添加策略类并注册到映射，不修改 Engine

### 接口定义

```python
class RepairStrategy(ABC):
    """Base class for all repair strategies."""

    @abstractmethod
    def generate(self, analysis: AnalysisResult) -> str:
        """Generate a repair prompt for the given analysis.
        
        Args:
            analysis: Structured analysis from the FailureAnalyzer.
            
        Returns:
            A repair prompt string that will be fed back to the LLM.
        """
        ...


class FeedbackEngine:
    """Complete feedback engine connecting Collector → Analyzer → Strategy."""

    def __init__(self, analyzer: FailureAnalyzer | None = None) -> None:
        """Initialize with default strategies mapped to all FailureTypes."""
        self._analyzer = analyzer or FailureAnalyzer()
        self._strategies: dict[FailureType, RepairStrategy] = {
            FailureType.SYNTAX_ERROR: SyntaxStrategy(),
            FailureType.IMPORT_ERROR: ImportStrategy(),
            FailureType.ASSERTION_ERROR: AssertionStrategy(),
            FailureType.TIMEOUT: TimeoutStrategy(),
            FailureType.RUNTIME_ERROR: RuntimeErrorStrategy(),
            FailureType.TEST_FAILURE: TestFailureStrategy(),
            FailureType.UNKNOWN: UnknownStrategy(),
        }

    def analyze(self, result: ToolResult) -> Feedback | None:
        """Analyze a tool result and produce feedback if there was a failure.
        
        Args:
            result: The ToolResult from a test execution.
            
        Returns:
            Feedback if a failure was detected, None if the result was successful.
        """
        collected = Collector.collect(result)
        analysis = self._analyzer.analyze(collected)
        if analysis.failure_type == FailureType.UNKNOWN and result.success:
            return None
        strategy = self._strategies[analysis.failure_type]
        prompt = strategy.generate(analysis)
        return Feedback(
            failure_type=analysis.failure_type,
            summary=f"{analysis.failure_type.value}: {analysis.error_message}",
            details=analysis,
            repair_prompt=prompt,
        )
```

### 每个 FailureType 的策略 Prompt 示例

| FailureType | 策略 Prompt 风格 |
|---|---|
| SYNTAX_ERROR | `"SyntaxError at {location}: {error_message}. Fix the syntax."` |
| IMPORT_ERROR | `"ImportError: {error_message}. Install missing module or fix import path."` |
| ASSERTION_ERROR | `"AssertionError: expected {expected} but got {actual}. Fix the logic."` |
| TIMEOUT | `"Timeout: {error_message}. Optimize algorithm or increase timeout."` |
| RUNTIME_ERROR | `"RuntimeError at {location}: {error_message}. Fix the error."` |
| TEST_FAILURE | `"Test failure: {error_message}. Review and fix the test or implementation."` |
| UNKNOWN | `"Unknown failure. Review the output and improve the code."` |

**注意：** 策略 Prompt 应该包含足够的信息让 LLM 理解问题，但不应该太长。目标是指导 LLM 进行有针对性的修复。

### 测试（TDD 红→绿）

```python
class TestRepairStrategies:
    def test_syntax_strategy(self) -> None:
        """SyntaxStrategy 生成的 prompt 包含 'SyntaxError' 和错误位置。"""
        ...

    def test_assertion_strategy_contains_values(self) -> None:
        """AssertionStrategy 生成的 prompt 包含预期值和实际值。"""
        ...

    def test_all_strategies_produce_different_prompts(self) -> None:
        """不同 FailureType 产生不同的 repair_prompt。"""
        ...


class TestFeedbackEngine:
    def test_success_result_returns_none(self) -> None:
        """exit_code=0 的 ToolResult → analyze() 返回 None。"""
        ...

    def test_failure_result_returns_feedback(self) -> None:
        """失败结果 → 返回 Feedback 对象。"""
        ...

    def test_feedback_contains_repair_prompt(self) -> None:
        """Feedback 对象的 repair_prompt 字段非空。"""
        ...

    def test_feedback_type_matches_analysis(self) -> None:
        """Feedback 的 failure_type 与分析结果一致。"""
        ...
```

---

## 与已有代码的集成方式

FeedbackEngine 完成后，T18 会将其接入 Main Loop：

```python
# T18 集成后的 Loop 流程（仅供参考，不需要在 T17 实现）
result = self.dispatcher.dispatch(tool_call)
if result.exit_code is not None:  # 测试命令
    feedback = self.feedback_engine.analyze(result)
    if feedback:
        ctx = self.context_manager.append_feedback(ctx, feedback)
```

---

## DoD

```
□ 所有测试通过（新增 + 原有 191 个）
□ 测试覆盖全部 7 种 FailureType
□ 使用策略映射（dict[FailureType, RepairStrategy]），不使用 if/elif 链
□ 成功结果返回 None
□ 代码风格与已有代码一致（from __future__ import annotations, 类型提示完整）
```