# Task 描述：T3T4T6

你正在实现 **TDD Coding Harness** 项目的 **T3（CLI 入口）、T4（LLM 抽象层 + Mock + Factory）、T6（BaseTool + Dispatcher）**。

## 背景

T1（脚手架）和 T2（数据模型 + Config）已完成。你可以在已有代码上直接开发。

已有代码位置：`E:\MyClass\2-2-summer\AI-Enabled_SoftwareEngineerBootcamp\homework\`

**关键文件：**
- `src/harness/models.py` — 13 个 pydantic 模型（Message, ToolCall, ToolResult, LLMResponse 等）
- `src/harness/config.py` — Config 加载（YAML + 默认值 + CLI 覆盖）
- `src/harness/cli.py` — CLI 存根（已有 `run` 和 `demo` 命令框架）
- `pyproject.toml` — 入口点已注册 `tdd-harness = "harness.cli:app"`

**测试风格：** 使用 `pytest`，类组织测试（`class TestXxx`），方法名 `test_xxx`，类型提示完整。

---

## T3：CLI 入口

**目标：** 补全 CLI 命令，支持 `--config`、`--provider`、`--model` 参数，接入 Config 加载。

**涉及文件：**
- `src/harness/cli.py`（修改）
- `src/harness/config.py`（已有，无需修改）
- `src/tests/test_cli.py`（新建）

**实现要点：**
1. `run` 命令增加 `--config`（Path | None）、`--provider`（str | None）、`--model`（str | None）选项
2. CLI 内部调用 `Config.from_yaml()` 加载配置文件，再用 `with_overrides()` 合并 CLI 参数
3. `demo` 命令保持 `guardrail`、`feedback`、`memory` 子命令占位

**必须先写测试（TDD 红→绿）：**
```python
class TestCliRun:
    def test_run_help(self) -> None:
        """`tdd-harness run --help` 应显示帮助信息，包含 task 参数和 --provider/--model 选项。"""
        # 使用 typer.testing.CliRunner
        from typer.testing import CliRunner
        from harness.cli import app
        runner = CliRunner()
        result = runner.invoke(app, ["run", "--help"])
        assert result.exit_code == 0
        assert "task" in result.stdout
        assert "--provider" in result.stdout
        assert "--model" in result.stdout

    def test_run_with_provider_override(self) -> None:
        """传入 --provider mock 应覆盖 config 中的 provider.name。"""
        ...

    def test_run_with_config(self) -> None:
        """传入 --config custom.yaml 应从指定路径加载配置。"""
        ...

class TestCliDemo:
    def test_demo_help(self) -> None:
        """`tdd-harness demo --help` 应显示子命令。"""
        ...
```

---

## T4：LLMProvider 抽象 + MockProvider + ProviderFactory

**目标：** 实现 LLM 抽象层，支持 Mock 和未来可扩展的 Provider。

**涉及文件：**
- `src/providers/base.py`（新建）
- `src/providers/mock.py`（新建）
- `src/providers/factory.py`（新建）
- `src/providers/__init__.py`（已有）
- `src/tests/test_providers.py`（新建）

**实现要点：**
1. `LLMProvider` 抽象基类（ABC），定义 `generate(messages, tools, config) → LLMResponse`
2. `MockProvider` 持有 `preset_responses: dict[str, LLMResponse]`，按输入消息匹配，不匹配返回默认响应
3. **`ProviderFactory` 使用注册机制**：`ProviderFactory.register("mock", MockProvider)`，`ProviderFactory.create(config) → LLMProvider`
4. 错误边界：`LLMTimeoutError`、`LLMAuthError`、`LLMRateLimitError`（定义在 `base.py` 中）

**必须先写测试（TDD 红→绿）：**
```python
class TestMockProvider:
    def test_returns_preset_response(self) -> None:
        """输入特定消息 → 返回预设的 LLMResponse。"""
        ...

    def test_returns_default_when_no_match(self) -> None:
        """未预设的消息 → 返回默认响应。"""
        ...

    def test_tool_call_in_response(self) -> None:
        """Mock 返回包含 ToolCall 的响应。"""
        ...

class TestProviderFactory:
    def test_creates_mock_provider(self) -> None:
        """Factory.create(name="mock") → MockProvider 实例。"""
        ...

    def test_creates_openai_provider(self) -> None:
        """Factory.create(name="openai") → OpenAICompatibleProvider 实例。"""
        ...

    def test_unknown_provider_raises(self) -> None:
        """Factory.create(name="unknown") → 抛出 ValueError。"""
        ...
```

---

## T6：BaseTool + ToolDispatcher

**目标：** 建立工具抽象和分发机制。

**涉及文件：**
- `src/tools/base.py`（新建）
- `src/tools/dispatcher.py`（新建）
- `src/tools/__init__.py`（已有）
- `src/tests/test_dispatcher.py`（新建）

**实现要点：**
1. `BaseTool(ABC)` 定义 `name: str`、`description: str`、`input_schema: dict` **类属性** + `execute(arguments: dict) → ToolResult` 抽象方法
2. Tool 自描述：Dispatcher 注册时自动读取 `tool.name`，避免 `register("read_file", WriteFile())` 这类错误
3. `ToolDispatcher` 维护 `name→tool` 映射，`register(tool: BaseTool)` 和 `dispatch(tool_call: ToolCall) → ToolResult` 方法
4. 未注册的工具返回 `ToolResult(success=False, error="Unknown tool: xxx")`

**必须先写测试（TDD 红→绿）：**
```python
class TestBaseTool:
    def test_tool_has_metadata(self) -> None:
        """BaseTool 子类应有 name / description / input_schema 类属性。"""
        ...

class TestToolDispatcher:
    def test_register_and_dispatch(self) -> None:
        """注册工具 → 分发 → 验证正确执行。"""
        ...

    def test_unknown_tool_returns_error(self) -> None:
        """未注册的工具 → 返回 error ToolResult。"""
        ...

    def test_dispatcher_routes_by_name(self) -> None:
        """注册多个工具 → 按名称路由到正确工具。"""
        ...
```

---

## DoD

```
□ pytest 测试通过（全部）
□ 测试覆盖核心逻辑 + 边界条件
□ 类型提示完整
□ 代码风格与已有代码一致（pydantic v2, class-based tests）
```

## 开发环境

- Python 3.12（`D:\Python3.12\python.exe`）
- 工作目录：`E:\MyClass\2-2-summer\AI-Enabled_SoftwareEngineerBootcamp\homework\`
- 运行测试：`D:\Python3.12\python.exe -m pytest src/tests/ -v`